<Page x:Class="Aquila.Views.Pages.ExplorerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Aquila.Views.Pages"
    xmlns:viewModels="clr-namespace:Aquila.ViewModels.Pages"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:helpers="clr-namespace:Aquila.Helpers"
    mc:Ignorable="d"
    d:DesignHeight="800" d:DesignWidth="1000"
    Title="Explorer"
    Style="{StaticResource BasePageStyle}"
    ScrollViewer.VerticalScrollBarVisibility="Auto"
    ScrollViewer.HorizontalScrollBarVisibility="Disabled">

    <Page.Resources>
        <helpers:HardwareTypeToIconConverter x:Key="HardwareTypeToIconConverter"/>

        <HierarchicalDataTemplate DataType="{x:Type viewModels:ExplorerTreeItem}" ItemsSource="{Binding Children}">
            <StackPanel Margin="0,4,0,4">
                <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Subtitle}" FontSize="11" Opacity="0.7" Margin="0,1,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Identifier}" FontSize="10" Opacity="0.5" Margin="0,1,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </HierarchicalDataTemplate>

        <Style TargetType="TreeViewItem">
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="20,12,20,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="LibreHardwareMonitor Explorer" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Raw reader view: hardware → subhardware → sensors" FontSize="12" Opacity="0.6" Margin="0,2,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <ui:Button Content="Export report" Command="{Binding ViewModel.ExportReportCommand}" VerticalAlignment="Center" Margin="0,0,8,0">
                        <ui:Button.Icon>
                            <ui:SymbolIcon Symbol="DocumentText24"/>
                        </ui:Button.Icon>
                    </ui:Button>
                </StackPanel>
            </Grid>
        </Border>

        <ui:TextBox Grid.Row="1"
                    Margin="20,4,20,8"
                    PlaceholderText="Search hardware, subhardware or sensors…"
                    ClearButtonEnabled="True"
                    Text="{Binding ViewModel.SearchText, UpdateSourceTrigger=PropertyChanged}">
            <ui:TextBox.Icon>
                <ui:SymbolIcon Symbol="Search24"/>
            </ui:TextBox.Icon>
        </ui:TextBox>

        <Grid Grid.Row="2" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Column="0" Padding="12" Margin="0,0,8,0">
                <TreeView ItemsSource="{Binding ViewModel.FilteredItems}" Background="Transparent" BorderThickness="0" Padding="4"/>
            </ui:Card>

            <ui:Card Grid.Column="1" Padding="12" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Report Sections" FontSize="13" FontWeight="SemiBold"/>
                    <ComboBox ItemsSource="{Binding ViewModel.ReportSectionKeys}" SelectedItem="{Binding ViewModel.SelectedReportSection}" Margin="0,6,0,6"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Height="400">
                        <TextBox Text="{Binding ViewModel.SelectedReportText}" TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True" Background="Transparent" BorderThickness="0"/>
                    </ScrollViewer>
                </StackPanel>
            </ui:Card>
        </Grid>
    </Grid>
</Page>
