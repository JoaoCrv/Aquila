<UserControl x:Class="Aquila.Controls.LabeledTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="140" d:DesignWidth="140">
    <!-- A title placed around any content (gauge, sparkline, ...). Title font/colour inherit from
         the theme/context; only placement (Top/Bottom/Left/Right) is configurable. -->
    <DockPanel x:Name="Root" LastChildFill="True">
        <TextBlock x:Name="TitleText"
                   DockPanel.Dock="Top"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="11" Opacity="0.6"
                   Margin="0,0,0,4"
                   TextTrimming="CharacterEllipsis"/>
        <ContentPresenter x:Name="Body"/>
    </DockPanel>
</UserControl>
