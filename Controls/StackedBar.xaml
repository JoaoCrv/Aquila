<UserControl x:Class="Aquila.Controls.StackedBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="220" d:DesignHeight="8">
    <!-- A bar split into N proportional coloured segments (e.g. RAM's physical/virtual pool
         composition) — sibling of SensorBar, but for several shares of a whole instead of one
         sensor against a scale. Segments always fill the full width; code-behind builds one Grid
         star-column per segment, so the layout is proportional without any custom width math. -->
    <Border CornerRadius="4" ClipToBounds="True" Background="{DynamicResource ControlFillColorSecondaryBrush}">
        <Grid x:Name="SegmentsHost"/>
    </Border>
</UserControl>
